import React from 'react';
import SVG, {icons} from '../ui/atoms/SVG'

export default {
    title: 'Icon',
    component: SVG
};

export const Icon = () => (
    <span>
        <SVG width={30}/>
    </span>
);

